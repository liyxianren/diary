# 心灵守护者 - 部署指南

## 🚀 快速部署到 Zeabur

### 前置要求
- GitHub 账号
- Zeabur 账号
- 本地 Git

### 部署步骤

#### 1. 准备项目文件
确保项目根目录包含以下文件：
```
diary/
├── app.py                    # 主应用文件
├── requirements.txt          # Python依赖
├── zeabur_config.yaml       # Zeabur配置
├── .env.example             # 环境变量示例
├── config.py                # 配置文件
├── models.py                # 数据库模型
├── extensions.py            # Flask扩展
├── routes/                  # 路由目录
├── static/                  # 静态文件
├── templates/               # HTML模板
└── study_web/              # 教学网站
```

#### 2. 推送到 GitHub

```bash
# 添加所有文件到Git
git add .

# 提交更改
git commit -m "feat: 完成心灵守护者项目，准备部署到Zeabur

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude <noreply@anthropic.com>"

# 推送到GitHub
git push origin main
```

#### 3. 在 Zeabur 创建项目

1. 登录 [Zeabur](https://zeabur.com)
2. 点击 "新建项目"
3. 选择 "导入Git仓库"
4. 授权GitHub并选择你的仓库
5. 选择 "Python" 模板

#### 4. 配置部署设置

##### 基本配置
- **项目名称**: 心灵守护者
- **构建命令**: `pip install -r requirements.txt`
- **启动命令**: `python app.py`
- **端口**: 5000

##### 环境变量设置
在 Zeabur 项目设置中添加以下环境变量：

```env
FLASK_ENV=production
PORT=5000
# 从 .env.example 中复制其他必要的环境变量
```

##### 数据库配置
1. 在项目中添加 MySQL 服务
2. 获取数据库连接信息
3. 更新环境变量：
```env
DB_HOST=your-db-host
DB_USER=your-db-user
DB_PASSWORD=your-db-password
DB_NAME=your-db-name
```

#### 5. 启动部署

1. 点击 "部署" 按钮
2. 等待构建完成
3. 部署成功后获取项目URL

### 🛠️ 故障排除

#### 常见问题

**Q: 构建失败，提示依赖安装错误**
A: 检查 `requirements.txt` 文件是否正确，确保所有依赖都有版本号

**Q: 数据库连接失败**
A: 确认数据库服务已启动，环境变量配置正确

**Q: 页面无法访问**
A: 检查启动命令和端口配置，确认 Flask 应用正常运行

**Q: 静态文件404**
A: 确保 Flask 配置了正确的静态文件路径

#### 调试命令
```bash
# 本地测试
python app.py

# 检查依赖
pip list

# 测试数据库连接
python -c "from config import create_app; app = create_app(); print('Database connected')"
```

### 📱 项目访问

部署成功后，你可以通过以下方式访问：

- **主应用**: `https://your-project.zeabur.app`
- **教学网站**: `https://your-project.zeabur.app/study_web`
- **API文档**: `https://your-project.zeabur.app/api`

### 🔧 自定义域名（可选）

1. 在域名提供商添加DNS记录
2. 在Zeabur项目设置中添加自定义域名
3. 配置SSL证书（自动）

### 📊 监控和维护

- Zeabur提供实时日志查看
- 设置数据库自动备份
- 定期更新依赖包
- 监控资源使用情况

### 🆘 获取帮助

- [Zeabur 文档](https://zeabur.com/docs)
- [Flask 部署指南](https://flask.palletsprojects.com/en/latest/deploying/)
- 项目Issues: 在GitHub仓库提交问题